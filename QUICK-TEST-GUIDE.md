# 🚀 TRIX 快速测试指南

## ✅ 已完成的功能

### 1. 登录界面优化
- ✅ **已删除浮动图片** - 登录界面更简洁清爽

### 2. 实时通讯功能
- ✅ **Supabase Realtime** - 实时消息推送
- ✅ **消息发送/接收** - 支持文字消息
- ✅ **未读消息计数** - 实时更新未读数
- ✅ **在线状态同步** - 好友在线状态实时显示
- ✅ **消息持久化** - 所有消息保存到数据库

---

## 📱 快速测试步骤

### 准备工作

**你需要:**
- 2 台设备 (手机、平板、电脑均可)
- 确保两台设备都能访问: `http://localhost:5173/` 或 `http://192.168.101.4:5173/`

### 测试账号

#### 设备 1 (Alice)
```
邮箱: alice@trix.app
密码: 123456
```

#### 设备 2 (Bob)
```
邮箱: bob@trix.app
密码: 123456
```

---

## 🧪 测试流程 (5分钟)

### Step 1: 注册/登录 (2分钟)

**设备 1:**
1. 打开浏览器访问 `http://192.168.101.4:5173/`
2. 如果账号不存在,点击"立即注册"
   - 用户名: `alice`
   - 邮箱: `alice@trix.app`
   - 密码: `123456`
3. 如果账号已存在,直接登录

**设备 2:**
1. 打开浏览器访问 `http://192.168.101.4:5173/`
2. 如果账号不存在,点击"立即注册"
   - 用户名: `bob`
   - 邮箱: `bob@trix.app`
   - 密码: `123456`
3. 如果账号已存在,直接登录

### Step 2: 查看好友列表 (30秒)

登录后,你应该能看到:
- ✅ 底部导航栏的"消息"图标
- ✅ 点击进入后,看到预置的好友列表
- ✅ Alice 应该能看到 Bob (如果已互为好友)
- ✅ Bob 应该能看到 Alice (如果已互为好友)

### Step 3: 发送实时消息 (2分钟)

**设备 1 (Alice):**
1. 点击 Bob 的头像,进入聊天界面
2. 输入消息: `嗨 Bob,我是 Alice!`
3. 点击发送按钮

**设备 2 (Bob):**
1. **不要点击任何东西** - 观察消息列表
2. 你应该看到:
   - ✅ Alice 的未读消息计数变为 `1`
   - ✅ 最后一条消息显示: `嗨 Bob,我是 Alice!`
   - ✅ 消息时间戳正确显示

3. 点击 Alice 的头像,进入聊天
4. 你应该看到:
   - ✅ Alice 发送的消息实时显示
   - ✅ 未读计数清零
   
5. 回复消息: `你好 Alice!我收到了你的消息 👋`

**设备 1 (Alice):**
1. **无需刷新** - 你应该能实时看到 Bob 的回复
2. 验证:
   - ✅ Bob 的消息立即显示在聊天界面
   - ✅ 消息顺序正确 (时间从上到下)

### Step 4: 测试未读计数 (30秒)

**设备 2 (Bob):**
1. 点击返回按钮,回到消息列表
2. 连续发送 3 条消息给 Alice

**设备 1 (Alice):**
1. 停留在首页 (不要进入聊天)
2. 点击底部"消息"按钮
3. 验证:
   - ✅ Bob 的未读计数显示为 `3`
   - ✅ 点击进入聊天后,未读计数变为 `0`

---

## ✅ 功能验证清单

完成以上测试后,请确认:

### 基础功能
- [ ] 登录界面简洁 (无浮动图片)
- [ ] 注册功能正常
- [ ] 登录功能正常

### 实时通讯
- [ ] 消息实时发送 (不需要刷新)
- [ ] 消息实时接收 (不需要刷新)
- [ ] 消息顺序正确
- [ ] 时间戳正确显示

### 未读消息
- [ ] 未读计数实时更新
- [ ] 进入聊天后未读计数清零
- [ ] 最后一条消息正确显示

### 用户体验
- [ ] 界面流畅无卡顿
- [ ] 消息发送后立即显示
- [ ] 返回键功能正常

---

## 🎯 已实现的技术方案

### 通讯架构

```
设备 1 (Alice)                Supabase Cloud               设备 2 (Bob)
     │                              │                            │
     │──── 发送消息 ───────────────>│                            │
     │     (INSERT into              │                            │
     │      chat_messages)           │                            │
     │                               │                            │
     │                               │<──── Realtime 订阅 ────────│
     │                               │     (监听 INSERT 事件)     │
     │                               │                            │
     │                               │──── 推送新消息 ──────────>│
     │                               │                            │
     │<──── 实时接收 ────────────────│                            │
     │     (WebSocket 推送)          │                            │
```

### 数据库表

1. **users** - 用户信息
2. **friends** - 好友关系
3. **chat_messages** - 聊天消息 (支持实时订阅)
4. **unread_counts** - 未读消息计数

### 实时订阅代码

```typescript
// 在 ChatDetail.tsx 中
supabase
  .channel(`chat:${friendId}`)
  .on('postgres_changes', {
    event: 'INSERT',
    schema: 'public',
    table: 'chat_messages',
    filter: `friend_id=eq.${friendId}`
  }, (payload) => {
    // 实时接收新消息
    const newMessage = payload.new as ChatMessage;
    setMessages(prev => [...prev, newMessage]);
  })
  .subscribe();
```

---

## 🐛 常见问题

### Q: 看不到对方发送的消息?

**A:** 检查以下几点:
1. 确保两台设备都连接到同一个网络
2. 检查 Supabase 配置是否正确
3. 打开浏览器控制台,查看是否有错误

### Q: 未读计数不更新?

**A:** 这是因为:
1. 需要返回到消息列表才能看到未读计数
2. 在聊天界面内,消息是实时接收的,不需要计数

### Q: 消息发送后没有反应?

**A:** 请检查:
1. 网络连接是否正常
2. Supabase 数据库是否可访问
3. 浏览器控制台是否有错误提示

---

## 📊 测试数据

如果需要预置的聊天记录,可以使用:

- **alice** 和 **bob** 已经有一些历史聊天记录
- 其他测试账号: carol, david, emma (密码都是 123456)

---

## 🎉 测试完成!

如果所有功能都正常,恭喜你!实时通讯功能已经成功实现!

**接下来可以测试的功能:**
- 语音输入 (如果设备支持)
- 添加新好友
- 多人群聊 (待开发)
- 发送图片/表情 (待开发)

---

**最后更新**: 2026-02-10  
**版本**: v1.0.0
