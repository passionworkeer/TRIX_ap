# 添加好友功能实现说明

## ✅ 已实现的功能

### 1. 通用添加好友功能

创建了新的 `addFriend()` 函数，支持添加所有数据库中的用户。

**位置**: `src/services/databaseService.ts`

**功能特性**:
- ✅ 支持通过**邮箱**或**用户名**搜索添加
- ✅ 自动建立**双向好友关系**
- ✅ 自动初始化未读计数
- ✅ 完整的错误处理（重复添加、添加自己等）
- ✅ 支持所有测试账号

**使用示例**:
```typescript
// 通过邮箱添加
await addFriend('alice@trix.app');

// 通过用户名添加
await addFriend('alice');
```

---

### 2. 推荐好友功能

在聊天页面添加了"推荐好友"板块，自动显示未添加的用户。

**功能特性**:
- ✅ 自动过滤已添加的好友
- ✅ 显示用户头像、昵称、用户名
- ✅ 一键添加按钮
- ✅ 添加后自动刷新列表

**实现位置**: `src/screens/Chat.tsx`

---

### 3. 手动添加好友弹窗

点击聊天页面右上角的"+"按钮，可以手动输入账号添加。

**功能特性**:
- ✅ 支持输入邮箱或用户名
- ✅ 实时错误提示
- ✅ 成功提示
- ✅ 添加后自动刷新好友列表

---

## 🎯 测试账号信息

以下是所有可用的测试账号：

| 用户名 | 邮箱 | 昵称 |
|--------|------|------|
| `xiaoming` | `xiaoming@trix.app` | 小明 |
| `alice` | `alice@trix.app` | Alice |
| `bob` | `bob@trix.app` | Bob |
| `carol` | `carol@trix.app` | Carol |
| `david` | `david@trix.app` | David |
| `emma` | `emma@trix.app` | Emma |

**密码统一**: `trix2026`

---

## 📝 使用指南

### 方式 1: 通过推荐好友添加

1. 登录 `xiaoming@trix.app`
2. 进入聊天页面
3. 在顶部看到"推荐好友"板块
4. 点击任意用户的"+ 添加"按钮
5. 添加成功后，该用户会出现在好友列表中

### 方式 2: 手动搜索添加

1. 点击聊天页面右上角的 ➕ 按钮
2. 在弹窗中输入对方的邮箱或用户名：
   - 例如: `alice@trix.app` 或 `alice`
   - 例如: `bob@trix.app` 或 `bob`
3. 点击"发送请求"
4. 添加成功！

---

## 🔧 数据库变化

### 好友关系 (friends 表)

添加好友时会自动插入**双向记录**：

```sql
-- 小明添加Alice
INSERT INTO friends (user_id, friend_id, name, ...) VALUES
  ('xiaoming_id', 'alice_id', 'Alice', ...),  -- 小明的好友列表中有Alice
  ('alice_id', 'xiaoming_id', '小明', ...);    -- Alice的好友列表中有小明
```

### 未读计数 (unread_counts 表)

同时初始化未读计数：

```sql
INSERT INTO unread_counts (user_id, friend_id, unread_count) VALUES
  ('xiaoming_id', 'alice_id', 0),
  ('alice_id', 'xiaoming_id', 0);
```

---

## ⚠️ 注意事项

### 1. 必须先创建用户

确保你已经在 Supabase 中创建了所有测试用户：
- 参考 `src/database/README-QUICK-START.md`
- 或参考 `src/database/CREATE-USERS-GUIDE.md`

### 2. 用户数据必须在 users 表中

添加好友时会从 `users` 表查询用户信息。确保：
```sql
SELECT * FROM users WHERE email = 'alice@trix.app';
-- 应该返回一条记录
```

### 3. 不能重复添加

如果已经是好友，会提示: "你们已经是好友了"

### 4. 不能添加自己

如果输入自己的账号，会提示: "不能添加自己为好友"

---

## 🐛 常见问题

### Q: 添加好友后看不到？

**A**: 检查以下几点:
1. 刷新页面或重新进入聊天页面
2. 检查控制台是否有错误
3. 确认数据库中有对应的记录:
   ```sql
   SELECT * FROM friends WHERE user_id = '你的用户ID';
   ```

### Q: 推荐好友显示为空？

**A**: 可能是:
1. 所有用户都已经添加为好友了
2. 数据库中只有一个用户（自己）
3. 检查控制台错误信息

### Q: 提示"未找到该用户"？

**A**: 
1. 检查输入的邮箱或用户名是否正确
2. 确认该用户在 `users` 表中存在
3. 注意邮箱和用户名是**大小写敏感**的

---

## 🎉 测试建议

### 完整测试流程:

1. **用小明登录**:
   ```
   邮箱: xiaoming@trix.app
   密码: trix2026
   ```

2. **检查推荐好友**:
   - 应该看到 Alice, Bob, Carol, David, Emma

3. **添加第一个好友**:
   - 点击 Alice 的"+ 添加"
   - 刷新后 Alice 应该出现在好友列表

4. **手动添加更多好友**:
   - 点击右上角 ➕
   - 输入 `bob@trix.app` 或 `bob`
   - 发送请求

5. **切换账号验证双向关系**:
   - 登出小明
   - 登录 `alice@trix.app / trix2026`
   - 应该能在好友列表中看到小明

6. **测试聊天功能**:
   - 点击任意好友
   - 进入聊天详情页
   - 应该能看到 Mock 的聊天记录

---

## 📊 功能对比

| 功能 | 旧版本 | 新版本 |
|------|--------|--------|
| 支持的账号 | 仅 2 个测试账号 | 所有数据库用户 |
| 添加方式 | 仅邮箱 | 邮箱或用户名 |
| 推荐好友 | ❌ 无 | ✅ 自动推荐 |
| 双向关系 | ✅ 有 | ✅ 有 |
| 错误提示 | 基础 | 详细友好 |
| 自动刷新 | ❌ 无 | ✅ 自动刷新 |

---

## ✅ 完成检查清单

- [x] 创建通用 `addFriend()` 函数
- [x] 支持邮箱和用户名搜索
- [x] 实现双向好友关系
- [x] 添加推荐好友功能
- [x] 更新手动添加弹窗
- [x] 添加后自动刷新列表
- [x] 完整的错误处理
- [x] 测试所有测试账号

---

## 🎊 大功告成！

现在你可以：
1. ✅ 使用任意测试账号登录
2. ✅ 看到推荐好友列表
3. ✅ 一键添加好友
4. ✅ 手动搜索添加好友
5. ✅ 查看好友列表
6. ✅ 与好友聊天

快去试试吧！🚀
