interface:
  display_name: "TRIX Canvas"
  short_description: "短剧分镜生成、排队和画布编排工作台"
  brand_color: "#33D1B2"
  default_prompt: "Use trix-canvas-skill to parse a short-drama script, queue storyboard image or video scenes, and manage the results on the canvas."

policy:
  allow_implicit_invocation: true
