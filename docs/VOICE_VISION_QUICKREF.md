# 🎤📷 TRIX 语音与视觉功能 - 快速参考

## 🚀 新功能总览

### 🎤 语音转文字 (Voice-to-Text)
**位置:** 聊天界面 (ChatDetail)  
**快捷键:** 点击输入框右侧的 🎤 麦克风按钮

#### 使用流程
1. 点击麦克风 → 按钮变红并闪烁
2. 开始说话 → 实时显示识别文字
3. 说完后停止 → 文字自动填充到输入框
4. 点击发送 → 命令通过 Gateway 发送到电脑

#### 状态指示
- ⚪ 灰色麦克风 = 空闲,可点击
- 🔴 红色闪烁 = 正在监听
- 🎤 "正在监听..." = 输入框提示

---

### 📷 视觉识别 (Camera Vision)
**位置:** Snapshot 界面 (底部导航 → 快照图标)  
**功能:** 实时相机预览 + 拍照分析

#### 使用流程
1. 进入 Snapshot → 自动启动相机
2. 对准物体 → 实时预览画面
3. 点击拍照按钮 📷 → 捕获当前画面
4. 确认照片 → 进入 AI 分析 (后续集成)

#### 相机模式
- 🟢 **实时模式** - 真实相机预览
- 🟡 **演示模式** - 相机不可用时自动降级

#### 功能按钮
- 🔄 切换摄像头 (前置/后置)
- ❌ 重拍 (删除照片)
- ✅ 确认分析 (提交照片)

---

## 📱 界面操作

### ChatDetail - 语音输入
```
┌─────────────────────────────┐
│  < Clawdbot Gateway      ⋮ │  ← 头部
├─────────────────────────────┤
│                             │
│  [机器人消息]               │
│               [用户消息]     │  ← 消息区
│                             │
├─────────────────────────────┤
│ 🖼️  [输入框...] 🎤  [发送] │  ← 输入栏
└─────────────────────────────┘
     ↑          ↑      ↑
    图片       语音    发送
```

**点击 🎤 语音按钮:**
- 首次使用会请求麦克风权限
- 按钮变红并闪烁 = 正在录音
- 再次点击 = 停止录音

---

### Snapshot - 相机拍照
```
┌─────────────────────────────┐
│  <  快照  🟢实时       🔄   │  ← 头部
├─────────────────────────────┤
│                             │
│     [实时相机画面]           │  ← 视频预览
│                             │
│      ┌───────────┐          │
│      │   [取景框]  │          │  ← 取景框
│      └───────────┘          │
│                             │
│   点击拍摄以分析 •           │  ← 提示
│                             │
│         (📷)               │  ← 拍照按钮
└─────────────────────────────┘
```

**拍照后:**
```
┌─────────────────────────────┐
│     [照片预览大图]           │
│                             │
│  [❌ 重拍]  [✅ 确认分析]   │
└─────────────────────────────┘
```

---

## 🔧 技术细节

### 语音识别
- **API:** Web Speech API
- **语言:** 中文 (zh-CN) + 英文
- **模式:** 实时转写,即说即显示
- **支持:** Chrome ✅, Edge ✅, Safari ✅, Firefox ⚠️

### 相机功能
- **API:** MediaDevices API
- **分辨率:** 1920x1080 (Full HD)
- **格式:** JPEG (质量 95%)
- **输出:** Blob + Data URL
- **支持:** Chrome ✅, Edge ✅, Safari ✅, Firefox ✅

---

## ⚠️ 常见问题

### 语音识别

**Q: 点击麦克风没反应?**
```
✅ 检查麦克风权限 (浏览器地址栏锁图标)
✅ 确保使用 HTTPS 或 localhost
✅ 打开控制台 (F12) 查看错误
```

**Q: 识别不准确?**
```
✅ 说话清晰,速度适中
✅ 减少背景噪音
✅ 确保网络连接正常
```

**Q: 支持哪些语言?**
```
✅ 中文 (默认)
✅ 英文
✅ 可在代码中修改 lang: 'en-US'
```

---

### 相机功能

**Q: 相机无法启动?**
```
✅ 允许相机权限
✅ 确保没有其他应用占用相机
✅ 使用 HTTPS 或 localhost
✅ 检查设备是否有摄像头
```

**Q: 只显示"演示模式"?**
```
⚠️ 相机权限被拒绝 → 重新授权
⚠️ 相机被占用 → 关闭其他应用
⚠️ 浏览器不支持 → 更换浏览器
```

**Q: 切换摄像头无效?**
```
⚠️ 设备只有一个摄像头
✅ 刷新页面重试
```

---

## 🎯 代码示例

### 在其他组件中使用语音

```typescript
import { useSpeechToText } from '../src/hooks/useSpeechToText';

function MyComponent() {
  const { 
    isListening, 
    transcript, 
    startListening, 
    stopListening 
  } = useSpeechToText({
    lang: 'zh-CN',
    onResult: (text) => {
      console.log('识别结果:', text);
    }
  });

  return (
    <button onClick={startListening}>
      {isListening ? '停止' : '开始'} 录音
    </button>
  );
}
```

### 在其他组件中使用相机

```typescript
import { useCamera } from '../src/hooks/useCamera';

function MyComponent() {
  const { 
    videoRef, 
    isReady, 
    capture, 
    capturedPhoto 
  } = useCamera({
    onCapture: (url, blob) => {
      console.log('照片URL:', url);
      console.log('文件大小:', blob.size);
    }
  });

  return (
    <>
      <video ref={videoRef} autoPlay />
      <button onClick={capture} disabled={!isReady}>
        拍照
      </button>
      {capturedPhoto && <img src={capturedPhoto.url} />}
    </>
  );
}
```

---

## 📊 浏览器兼容性

| 功能 | Chrome | Edge | Safari | Firefox | 备注 |
|------|--------|------|--------|---------|------|
| 语音识别 | ✅ | ✅ | ✅ | ⚠️ | Firefox 需配置 |
| 相机访问 | ✅ | ✅ | ✅ | ✅ | - |
| 切换摄像头 | ✅ | ✅ | ✅ | ✅ | - |
| 实时预览 | ✅ | ✅ | ✅ | ✅ | - |

**推荐浏览器:** Chrome / Edge (最佳兼容性)

---

## 🎨 UI 状态

### 语音按钮状态
| 状态 | 图标 | 颜色 | 动画 |
|------|------|------|------|
| 空闲 | 🎤 | 灰色 | 无 |
| 监听中 | 🚫 | 红色 | 脉冲 |
| 错误 | 🎤 | 灰色 | 无 |

### 相机状态指示
| 状态 | 标签 | 颜色 | 说明 |
|------|------|------|------|
| 实时 | 🟢 实时 | 绿色 | 真实相机 |
| 演示 | 🟡 演示 | 黄色 | 模拟模式 |
| 错误 | ❌ 错误 | 红色 | 权限/硬件问题 |

---

## 🔗 后续集成

### 语音命令
- [ ] 唤醒词 ("Hey TRIX")
- [ ] 连续对话
- [ ] 命令模板
- [ ] TTS 语音回复

### 视觉分析
- [ ] AI 物体识别
- [ ] OCR 文字识别
- [ ] 场景理解
- [ ] 实时标注

### 多模态
- [ ] 语音 + 视觉联动
- [ ] 手势控制
- [ ] 环境感知

---

## 📞 获取帮助

**查看完整文档:**
```
VOICE_VISION_INTEGRATION.md
```

**检查浏览器控制台:**
```
F12 → Console
搜索 "✅" 或 "❌" 查看日志
```

**重启应用:**
```bash
npm run dev
```

---

**版本:** v1.1.0  
**更新:** 2026年2月4日  
**功能:** 语音转文字 + 视觉识别
