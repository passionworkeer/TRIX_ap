# 项目文档 (docs)

此目录为自动/手动整理后的项目文档集合。默认由 `./scripts/organize_docs.ps1` 脚本生成（复制根目录下的 `.md` 文件到此处）。

说明：
- 根目录保留 `README.md` 作为项目入口。
- 子项目（例如 `TRIX_ap/`）和 `database/` 目录将保留其内部文档，不会被脚本移动。

请在确认无误后，再将副本替换为移动或做更细分的子目录整理。
