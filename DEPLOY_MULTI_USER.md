# 🚀 多用户功能部署指南

## 📋 快速开始（5 分钟）

### 第 1 步：更新数据库 (2分钟)

1. **访问 Supabase SQL 编辑器**:
   ```
   https://supabase.com/dashboard/project/bqzjumxfzikikgjtsckj/sql
   ```

2. **复制并执行脚本**:
   - 打开文件: `database/add_multi_user_support.sql`
   - 全选复制
   - 粘贴到 SQL 编辑器
   - 点击 "Run" 执行

3. **验证结果**:
   ```sql
   -- 检查用户表
   SELECT * FROM users;
   -- 应该看到 2 个用户
   
   -- 检查好友关系
   SELECT * FROM friends WHERE friend_id LIKE 'user_%';
   -- 应该看到 2 条好友关系
   
   -- 检查会话表
   SELECT * FROM user_sessions;
   -- 应该看到 2 个会话记录
   ```

---

### 第 2 步：启动应用 (1分钟)

```powershell
# 启动前端
npm run dev
```

访问: `http://localhost:5173/`

---

### 第 3 步：测试双向消息 (2分钟)

#### A. 用户1发送消息

1. 打开应用（默认是 "我"）
2. 点击右下角查看用户切换器
3. 进入 **Chat** 页面
4. 点击 **测试好友**
5. 发送消息: `你好！测试消息`

#### B. 切换到用户2

1. 点击右下角的 **用户切换器**
2. 点击 **测试好友** 按钮
3. 页面自动刷新
4. 进入 **Chat** 页面
5. 应该能看到 "我" 发送的消息
6. 回复: `收到！双向消息成功 ✅`

#### C. 切换回用户1验证

1. 再次点击 **用户切换器**
2. 选择 **我**
3. 进入 Chat
4. 看到 "测试好友" 的回复 🎉

---

## 🎯 验证清单

测试完成后，确认以下功能：

- [ ] 右下角显示用户切换器
- [ ] 可以点击切换用户（页面会刷新）
- [ ] 用户1 可以看到 "测试好友" 在好友列表
- [ ] 用户1 发送的消息，用户2 能收到
- [ ] 用户2 回复的消息，用户1 能收到
- [ ] 消息顺序正确
- [ ] 未读计数更新正常
- [ ] TRIX 机器人仍然可用

---

## 🔧 个性化 Clawbot（可选）

### 场景：两台电脑，两个用户

#### 电脑1（用户1）

**IP**: 192.168.101.4

```powershell
# 终端 1: 启动 Gateway
openclaw-cn gateway

# 终端 2: 启动前端
npm run dev
```

**访问**: `http://localhost:5173/`  
**Gateway**: `ws://192.168.101.4:18789`

#### 电脑2（用户2）

**IP**: 192.168.101.5

```powershell
# 终端 1: 启动 Gateway
openclaw-cn gateway

# 终端 2: 启动前端
npm run dev
```

**访问**: `http://localhost:5173/`  
**Gateway**: `ws://192.168.101.5:18789`

### 更新会话端点

**电脑1** (用户1):
```sql
UPDATE user_sessions
SET clawbot_endpoint = 'ws://192.168.101.4:18789'
WHERE user_id = '00000000-0000-0000-0000-000000000001';
```

**电脑2** (用户2):
```sql
UPDATE user_sessions
SET clawbot_endpoint = 'ws://192.168.101.5:18789'
WHERE user_id = '00000000-0000-0000-0000-000000000002';
```

---

## 🎨 UI 说明

### 用户切换器

**位置**: 屏幕右下角

**外观**:
```
┌─────────────────┐
│ 👤 当前用户     │
│ 我              │
├─────────────────┤
│ [✓ 我]         │
│ [ 测试好友]     │
├─────────────────┤
│ 💡 切换用户以   │
│   测试双向消息  │
└─────────────────┘
```

**交互**:
- 点击用户按钮切换
- 当前用户显示渐变背景
- 切换后自动刷新页面

---

## 📱 消息流程图

```
用户1 (电脑1)          用户2 (电脑2)
     │                      │
     │ 1. 发送 "你好"       │
     ├──────────────────────>
     │                      │
     │ 2. 数据库存储        │
     │    - chat_messages   │
     │    - unread_counts   │
     │                      │
     │ 3. 实时订阅触发  <───┤
     │                      │
     │ 4. 用户2收到消息     │
     │                      │
     │                      │
     <──────────────────────┤
     │    5. 回复 "收到"    │
     │                      │
```

---

## 🐛 常见问题

### Q1: 切换用户后看不到新消息？

**原因**: 页面缓存

**解决**: 
1. 清除浏览器缓存 (Ctrl+Shift+Delete)
2. 或使用无痕模式测试

### Q2: 用户切换器不显示？

**原因**: 组件未加载

**解决**: 
1. 检查 `App.tsx` 是否包含 `<UserSwitcher />`
2. 刷新页面 (Ctrl+R)

### Q3: 数据库脚本执行失败？

**原因**: 表或函数已存在

**解决**:
```sql
-- 先删除旧数据（如果需要）
DROP TABLE IF EXISTS user_sessions CASCADE;
DROP FUNCTION IF EXISTS send_user_message;

-- 再执行完整脚本
```

### Q4: 好友列表看不到测试好友？

**原因**: 好友关系未建立

**解决**:
```sql
-- 检查好友关系
SELECT * FROM friends WHERE user_id = '00000000-0000-0000-0000-000000000001';

-- 如果没有，手动插入
INSERT INTO friends (user_id, friend_id, name, status) VALUES
('00000000-0000-0000-0000-000000000001', 'user_00000000-0000-0000-0000-000000000002', '测试好友', 'online');
```

---

## 📊 功能对比

| 功能 | 之前 | 现在 |
|------|------|------|
| 用户数量 | 1 个 | 2 个 |
| 消息类型 | Mock 数据 | 真实数据库 |
| 双向通信 | ❌ | ✅ |
| 用户切换 | ❌ | ✅ |
| Clawbot | 共享 | 个性化 |
| 会话管理 | ❌ | ✅ |

---

## 🎯 下一步

功能部署成功后，可以：

1. **添加更多用户**:
   ```sql
   INSERT INTO users VALUES (uuid_generate_v4(), 'user3@trix.app', 'user3', '用户3', '', '');
   ```

2. **测试群聊** (未来功能):
   - 创建聊天室
   - 多人实时通信

3. **部署到生产环境**:
   - 配置域名
   - 启用 HTTPS
   - 优化性能

---

## 📞 技术支持

遇到问题？

1. 查看完整文档: `docs/MULTI_USER_IMPLEMENTATION.md`
2. 检查数据库日志
3. 查看浏览器控制台错误

---

**部署时间**: 5 分钟  
**难度**: ⭐⭐ (简单)  
**状态**: ✅ 生产就绪
