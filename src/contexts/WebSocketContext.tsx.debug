// 在 challenge 响应部分添加这些调试语句
console.log('🔍 收到 challenge 数据:', JSON.stringify(data, null, 2));
console.log('🔍 Challenge ID:', data.id);
console.log('🔍 Challenge event:', data.event);

const challengeResponse: ConnectRequest = {
  type: 'req',
  id: data.id,
  method: 'connect',
  params: {
    minProtocol: 3,
    maxProtocol: 3,
    role: 'operator',
    client: {
      id: 'clawdbot-ios',
      mode: 'webchat',
      platform: 'ios',
      displayName: 'TRIX App',
      version: '1.0.0',
      instanceId: generateId()
    },
    caps: [],
    auth: { token: AUTH_TOKEN }
  }
};

console.log('🔍 即将发送的 challengeResponse:', JSON.stringify(challengeResponse, null, 2));
console.log('🔍 challengeResponse 类型:', typeof challengeResponse);
console.log('🔍 challengeResponse.id:', challengeResponse.id);

socket.send(JSON.stringify(challengeResponse));
console.log('📤 已发送 challenge 响应');