// 替代方案：手动构建 JSON 字符串
const challengeResponseStr = JSON.stringify({
  type: 'req',
  id: data.id,
  method: 'connect',
  params: {
    minProtocol: 3,
    maxProtocol: 3,
    role: 'operator',
    client: {
      id: 'clawdbot-ios',
      mode: 'webchat',
      platform: 'ios',
      displayName: 'TRIX App',
      version: '1.0.0',
      instanceId: generateId()
    },
    caps: [],
    auth: { token: AUTH_TOKEN }
  }
});

console.log('🔍 手动构建的响应:', challengeResponseStr);
socket.send(challengeResponseStr);