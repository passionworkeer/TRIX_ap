import { describe, it } from 'vitest';

describe.skip('ChatDetail', () => {
  it('needs a lighter-weight test harness before it can run in Vitest on Windows', () => {
    // This placeholder keeps the legacy file discoverable without importing
    // the full ChatDetail dependency graph, which currently exhausts the
    // worker heap during module loading.
  });
});
