Desktop main-process configuration modules live here.

The smoke suite expects this directory to exist because desktop runtime
configuration is treated as a first-class surface alongside startup and IPC.
